{
  "name": "mcp-server-datadog",
  "version": "0.1.0",
  "description": "MCP server for Datadog — Logs, APM, Metrics, Monitors",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-server-datadog": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@datadog/datadog-api-client": "^1.52.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
